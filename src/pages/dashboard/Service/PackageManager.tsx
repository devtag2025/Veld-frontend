const PackageManager = () => {
  return (
    <div className="p-8 text-center border-2 border-dashed rounded-xl text-muted-foreground">
      Package Not Configured Yet.
    </div>
  );
};

export default PackageManager;
